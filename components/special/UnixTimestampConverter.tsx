"use client";
import { useState } from "react";
export default function UnixTimestampConverter(){const [ts,setTs]=useState(String(Math.floor(Date.now()/1000)));const [date,setDate]=useState(new Date().toISOString().slice(0,16));let readable="";const n=parseInt(ts||"0",10);if(!Number.isNaN(n)&&n>0) readable=new Date(n*1000).toISOString();let unix="";try{unix=String(Math.floor(new Date(date).getTime()/1000));}catch{}return <div className="form-panel"><div className="form-row"><div className="field"><label>Unix Timestamp</label><input value={ts} onChange={(e)=>setTs(e.target.value)} /></div><div className="field"><label>Date</label><input type="datetime-local" value={date} onChange={(e)=>setDate(e.target.value)} /></div></div><div className="result-box"><div className="result-label">Conversions</div><div className="helper">Timestamp to date: {readable || 'Enter a valid timestamp.'}</div><div className="helper">Date to timestamp: {unix || 'Enter a valid date.'}</div></div></div>;}
