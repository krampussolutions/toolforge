"use client";
import { jsPDF } from "jspdf";
import { useState } from "react";
export default function ImageToPdf(){const [status,setStatus]=useState("Upload one or more images.");async function fileToDataUrl(file:File){return await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=reject;r.readAsDataURL(file);});}async function handleFiles(files:FileList){const pdf=new jsPDF({unit:"pt",format:"a4"});let first=true;for(const file of Array.from(files)){const data=await fileToDataUrl(file);const img=new Image();img.src=data;await img.decode();const pw=pdf.internal.pageSize.getWidth();const ph=pdf.internal.pageSize.getHeight();const ratio=Math.min(pw/img.width,ph/img.height);const w=img.width*ratio,h=img.height*ratio,x=(pw-w)/2,y=(ph-h)/2;if(!first) pdf.addPage();pdf.addImage(data,"JPEG",x,y,w,h);first=false;}pdf.save("images-to-pdf.pdf");setStatus("PDF downloaded.");}return <div className="form-panel"><div className="field"><label>Upload images</label><input type="file" accept="image/*" multiple onChange={(e)=>{const files=e.target.files;if(files?.length) handleFiles(files);}} /></div><div className="result-box"><div className="result-value" style={{fontSize:"22px"}}>{status}</div></div></div>;}
