"use client";
import { useState } from "react";
export default function RandomLetterGenerator(){const [count,setCount]=useState("10");const [output,setOutput]=useState("");function gen(){const n=Math.max(1,Math.min(100,parseInt(count||"1",10)||1));const chars="ABCDEFGHIJKLMNOPQRSTUVWXYZ";let out="";for(let i=0;i<n;i++) out+=chars[Math.floor(Math.random()*chars.length)];setOutput(out);}return <div className="form-panel"><div className="field"><label>Letter Count</label><input value={count} onChange={(e)=>setCount(e.target.value)} /></div><button className="button" type="button" onClick={gen}>Generate Letters</button><div className="result-box"><div className="result-label">Output</div><div className="result-value" style={{fontSize:"22px"}}>{output || "Generated letters will appear here."}</div></div></div>;}
