"use client";
import { useMemo, useState } from "react";
export default function PasswordStrengthChecker(){const [input,setInput]=useState("StrongPass123!");const score=useMemo(()=>{let s=0;if(input.length>=8)s++;if(/[A-Z]/.test(input))s++;if(/[a-z]/.test(input))s++;if(/[0-9]/.test(input))s++;if(/[^A-Za-z0-9]/.test(input))s++;return s;},[input]);const label=score<=2?"Weak":score===3?"Fair":score===4?"Good":"Strong";return <div className="form-panel"><div className="field"><label>Password</label><input value={input} onChange={(e)=>setInput(e.target.value)} /></div><div className="result-box"><div className="result-label">Strength</div><div className="result-value" style={{fontSize:"22px"}}>{label}</div><div className="helper">Length: {input.length} • Score: {score}/5</div></div></div>;}
